RSpec.describe Capybara::Chromedriver::Logger do
  it "has a version number" do
    expect(Capybara::Chromedriver::Logger::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
